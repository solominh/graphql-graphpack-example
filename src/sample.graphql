query {
  users {
    id
    name
    email
    age
  }
}

query {
  user(id: 1) {
    id
    name
    email
    age
  }
}

mutation {
  createUser(id: 3, name: "Robert", email: "robert@gmail.com", age: 21) {
    id
    name
    email
    age
  }
}
